# C++ 구조체(struct)와 클래스(class) 정리

---

## 1. 구조체(struct)와 클래스(class)의 차이점

구조체와 클래스는 C++에서 사용자 정의 자료형을 만드는 두 가지 방법이다.  
문법적으로는 거의 동일하지만, **기본 접근 지정자(default access specifier)** 에서 결정적인 차이가 있다.

| 구분 | `struct` | `class` |
|------|----------|---------|
| 기본 접근 지정자 | `public` | `private` |
| 멤버 변수/함수 | 기본 공개 | 기본 비공개 |
| 상속 시 기본 접근 | `public` 상속 | `private` 상속 |
| 주요 사용 목적 | 단순 데이터 묶음 | 데이터 + 동작(캡슐화) |

### 정리

- `struct`는 멤버가 **기본적으로 public** 이므로 외부에서 직접 접근 가능하다.
- `class`는 멤버가 **기본적으로 private** 이므로 캡슐화(encapsulation)를 기본으로 지원한다.
- C++에서는 접근 지정자만 다를 뿐, 나머지 기능(생성자, 소멸자, 상속, 멤버 함수 등)은 `struct`와 `class` 모두 동일하게 사용할 수 있다.

---

## 2. 클래스(class)와 객체(object)의 차이점

| 구분 | 클래스 (class) | 객체 (object) |
|------|--------------|--------------|
| 정의 | 데이터와 함수를 묶은 **설계도(틀)** | 클래스로부터 생성된 **실체(인스턴스)** |
| 메모리 | 정의 자체는 메모리를 차지하지 않음 | 생성 시 실제 메모리를 차지함 |
| 비유 | 붕어빵 틀 | 붕어빵 |
| 개수 | 하나의 클래스 정의 | 클래스 하나로 여러 객체 생성 가능 |

### 정리

- **클래스**는 객체를 만들기 위한 **설계도**이며, 선언 자체만으로는 메모리를 할당하지 않는다.
- **객체**는 클래스를 기반으로 실제로 **생성된 실체**이며, 생성 시 메모리가 할당된다.
- 하나의 클래스로 여러 개의 객체를 만들 수 있으며, 각 객체는 서로 독립적인 멤버 변수를 가진다.

---

## 3. `sizeof(클래스명)`의 결과값과 설명

### 실행 결과

<img width="813" height="179" alt="Image" src="https://github.com/user-attachments/assets/5fff4278-395f-40ac-84c2-9e08e27cbdbf" />
```

### 결과 분석

| 클래스 | 예상 크기 | 실제 크기 | 이유 |
|--------|----------|----------|------|
| `A` | 0 bytes | **1 byte** | C++ 표준상 빈 클래스도 최소 1 byte 할당 (객체 주소 구별을 위해) |
| `B` | 12 bytes | **16 bytes** | `int`(4) + `double`(8) = 12 → 정렬(alignment)로 16 bytes |
| `C` | 13 bytes | **16 bytes** | `int`(4) + `char`(1) + 패딩(3) + `double`(8) = 16 bytes |

### 핵심 정리

1. **`sizeof`는 멤버 변수의 크기만 계산한다.**  
   → 멤버 함수는 코드 영역(text segment)에 저장되므로 `sizeof` 결과에 포함되지 않는다.

2. **빈 클래스의 크기는 0이 아닌 1 byte이다.**  
   → 서로 다른 객체가 서로 다른 주소를 가져야 하기 때문에 C++ 표준에서 최소 1 byte를 보장한다.

3. **메모리 정렬(Memory Alignment / Padding)이 적용된다.**  
   → CPU가 데이터를 효율적으로 읽기 위해, 컴파일러는 멤버 변수 사이에 **패딩(padding)** 을 삽입할 수 있다.  
   → 결과적으로 `sizeof` 값이 단순 합산보다 크게 나올 수 있다.

---

## 참고: 메모리 정렬(Padding) 개념 정리

```
클래스 C의 메모리 배치:
┌────────────┬──────────┬────────────────────┬────────────────────┐
│  int x (4) │ char c(1)│   padding (3 bytes) │   double y (8)     │
└────────────┴──────────┴────────────────────┴────────────────────┘
→ 총 4 + 1 + 3 + 8 = 16 bytes
```

> `double`은 8 byte 경계에 맞춰 배치되어야 하므로, `char` 뒤에 3 byte의 패딩이 자동 삽입된다.
